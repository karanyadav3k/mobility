# GatiConnect Environment Variables

# DATABASE_URL: Leave empty for local SQLite, or provide PostgreSQL URI for Cloud
# e.g. postgresql://user:password@hostname:5432/dbname
DATABASE_URL=

# PORT
PORT=8080

# ENVIRONMENT (development / production)
ENVIRONMENT=production
